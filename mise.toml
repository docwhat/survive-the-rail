[tasks]
build = "go build -o survive-the-rail ./cmd/main.go"
run = { depends = "build", run = "./survive-the-rail" }
test_quick = { depends = "build", run = "./survive-the-rail --quit-after-ms=1000" }
